name: discord-webhook-example
schema_version: 1
pipeline:
  name: discord-webhook-example
  stages:
    - name: publish
      factory:
        _target_: stages:PublishStage
      outputs:
        report:
          artifact_type: text
          codec_key: text.v1
