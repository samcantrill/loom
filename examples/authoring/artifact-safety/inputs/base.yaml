name: artifact-safety
pipeline:
  dataset:
    _include_: ./dataset.yaml
  service:
    endpoint: ${oc.env:LOOM_ARTIFACT_SAFETY_ENDPOINT,https://example.invalid/api}
    api_token: ${oc.env:LOOM_ARTIFACT_SAFETY_TOKEN}
  output:
    root: ${oc.env:LOOM_ARTIFACT_SAFETY_OUTPUT,/tmp/loom-artifact-safety}

