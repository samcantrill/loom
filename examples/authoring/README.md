# Authoring Examples

Trusted config authoring examples now live with the config package in
`packages/weave/examples`.

Root Loom examples focus on workflow/runtime behavior that consumes authored
config through Loom adapter paths.
