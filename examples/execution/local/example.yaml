id: execution.local
title: Local Pipeline Run
summary: Run a local two-stage pipeline and reuse unchanged stages from the same run directory.
capability: execution
level: introductory
introduced_in: v0
status: runnable
validation: smoke
entrypoints:
  - path: run_pipeline.py
    kind: python
    description: Run the pipeline twice and print stage actions plus artifact URIs.
tags:
  - local-run
  - artifacts
  - resume
