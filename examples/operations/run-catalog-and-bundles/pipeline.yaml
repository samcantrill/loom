name: run-catalog-and-bundles
schema_version: 1
variant: baseline
pipeline:
  name: run-catalog-and-bundles
  stages:
    - name: produce
      factory:
        _target_: stages:ProduceVariantStage
      config:
        variant: ${variant}
      outputs:
        payload:
          artifact_type: text
          codec_key: text.v1
