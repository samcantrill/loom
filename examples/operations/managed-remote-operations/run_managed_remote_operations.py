"""Name the remote operations journey; its E2E owns generated TLS setup."""

from __future__ import annotations

print("managed remote operations: use daemon-agents, daemon-agent, and guarded controls")
