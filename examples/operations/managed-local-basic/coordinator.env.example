# Owner-protected machine values. Copy to coordinator.env and replace each value.
LOOM_DEPLOYMENT_ROOT=/secure/loom/managed-local/deployment
LOOM_RUN_STORE_ROOT=/secure/loom/managed-local/runs
LOOM_MACHINE_ID=managed-local-machine
