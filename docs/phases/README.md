# Phase Artifacts Moved

Phase execution plans, PR bodies, blocker-resolution sidecars, and merge records
now live with their owning roadmap stage under:

```text
docs/roadmap/stage-<id>/phases/
```

Use the stage implementation plan at
`docs/roadmap/stage-<id>/implementation-plan.md` to find the current phase
status and phase artifact names.
