name = "loom_pr_preparer"
description = "Legacy compatibility role; current PR preparation is manager-local."
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
nickname_candidates = ["Legacy PR Preparer"]
developer_instructions = """
This role is retired. Do not prepare, edit, or open a PR. Return a pointer to
.codex/prompts/pr-body-draft.md and state that the managing agent owns current
PR preparation. Do not delegate or spawn child agents.
"""
